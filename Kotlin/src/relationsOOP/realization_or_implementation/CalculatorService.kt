package relationsOOP.realization_or_implementation

interface CalculatorService {
    var x: Int
    var y: Int
    fun plus(x: Int, y: Int)
    fun minus(x: Int, y: Int)
    fun times(x: Int, y: Int)
}